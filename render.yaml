envVarGroups:
  - name: divinecode-shared-secrets
    envVars:
      - key: NODE_ENV
        value: production
      - key: JUDGE_WORKER_SECRET
        generateValue: true
      - key: CRON_SECRET
        value: "my-secret-password-123" # Change this string to a secure token in your Render dashboard panel later

services:
  - type: keyvalue
    name: divinecode-redis
    plan: starter
    maxmemoryPolicy: noeviction
    ipAllowList: []

  - type: web
    name: divinecode-api
    runtime: node
    plan: starter
    buildCommand: npm install && npm run prisma:generate && npm run build --workspace apps/api
    preDeployCommand: npm run prisma:deploy
    startCommand: npm run start --workspace apps/api
    envVars:
      - fromGroup: divinecode-shared-secrets
      - key: ENABLE_API_WORKERS
        value: "false"
      - key: DATABASE_URL
        fromDatabase:
          name: divinecode-postgres
          property: connectionString
      - key: REDIS_URL
        fromService:
          type: keyvalue
          name: divinecode-redis
          property: connectionString
      - key: JUDGE0_URL
        value: https://emkc.org/api/v2/piston/execute
      - key: PISTON_URL
        value: https://emkc.org/api/v2/piston/execute

  - type: worker
    name: divinecode-worker
    runtime: node
    plan: starter
    buildCommand: npm install && npm run prisma:generate && npm run build --workspace apps/api
    startCommand: npm run worker:start --workspace apps/api
    envVars:
      - fromGroup: divinecode-shared-secrets
      - key: ENABLE_API_WORKERS
        value: "true"
      - key: DATABASE_URL
        fromDatabase:
          name: divinecode-postgres
          property: connectionString
      - key: REDIS_URL
        fromService:
          type: keyvalue
          name: divinecode-redis
          property: connectionString
      - key: JUDGE0_URL
        value: https://emkc.org/api/v2/piston/execute
      - key: PISTON_URL
        value: https://emkc.org/api/v2/piston/execute

databases:
  - name: divinecode-postgres
    databaseName: divinecode
    user: divinecode
    plan: starter